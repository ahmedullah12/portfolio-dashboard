export interface ISkill {
    _id: string;
    name: string;
    image: string;
    title: string;
};

export interface IBlog {
    _id: string;
    title: string;
    blogImage: string;
    text: string;
}